<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.teller.mapper.CommCodeMapper">

    <select id="selectCodeList" parameterType="string"
            resultType="com.study.teller.vo.CommCodeVo">
        SELECT GRP_CODE, CODE, CODE_NM, SORT_ORD, USE_YN
          FROM TB_COMM_CODE
         WHERE GRP_CODE = #{grpCode}
           AND USE_YN = 'Y'
         ORDER BY SORT_ORD
    </select>

    <select id="selectCodeNm" parameterType="com.study.teller.vo.CommCodeVo"
            resultType="string">
        SELECT CODE_NM
          FROM TB_COMM_CODE
         WHERE GRP_CODE = #{grpCode}
           AND CODE = #{code}
    </select>

</mapper>